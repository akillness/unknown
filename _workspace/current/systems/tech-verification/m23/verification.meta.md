---
updated: 2026-09-14
cycle: 20260909-preproduction-c7
status: current
supersedes: null
owner: game-qa
---

# M23 · verification.json

[OBSERVED] M23 초기35ebdc5·교정5d7b18c·마지막 Q6 off-reader 수명 교정의 실행 영수증. 현재 프로젝트 Unity187통과·Mac315파일/410875921B·ba14f79e…이며 이전177/186개와 빌드별 캡처는 그대로 보존한다. 사람0·강한 P1/P2 blocked·Windows/전체 완주 미측정 유지.

| 파일 | bytes | SHA-256 |
|---|---:|---|
| `verification.json` | 95624 | `cc64c59ef7466161d8f69aa81afebb5a2d6c5389c01a62f60039ec2aee92f84f` |

## 초기35ebdc5 전달 정리·동기화 이력

- [OBSERVED] `graphify update .`: AST2226/2226,39063노드/43237간선/4143community, 입력/출력 토큰0. HTML은5000노드 한도로 생략. 문서/이미지 의미 추출은 실행하지 않았다.
- [OBSERVED] `zg index .` succeeded. 후속 prescribed session-start에서2056/2056파일·105158entities·190truncated fragments·대기/실패0을 관찰했다. 로컬 potion-code-16m-v2이며 원격 임베딩을 허용하지 않았다. truncation을 완전 검색 보장으로 확대하지 않는다.
- [OBSERVED] mex-agent는 최종 identity probe에서도 unavailable; TeX `mex`를 실행하지 않았다. Obsidian CLI는 앱 부재로 exit1이어서 허용된 파일 fallback으로 기존 project decisions/index/log만 갱신했다. 전체 G8 memory gate PASS가 아니다.
- [OBSERVED] prescribed session-start의 freshness는674개 Markdown/0 findings였다. frontmatter/supersedes만 검사하며 시점·memory_sync 전체는 미검사다.
- [OBSERVED] 정식 결함 C7-F56..F60은 Q1..Q5의 상태 정본이다. 기존 생성기로 대장170행/dropped0과 덱36장을 동기화했고 실제 브라우저 s19의63개 셀이 전부 일치했다. 문서/산술 검증514/514 SPEC-PASS, runtime NOT-MEASURED의 판정 범위를 유지했다.
- [DELIVERY] 기존 M14–M21/Aside 원시 `.log`53개는 로컬에 그대로 보존하고 stage에서 제외한다. 소스·기존 리소스/출처·XML·선별 실행 증거는 명시 경로로 전달한다. 앱 전체·로컬 체크포인트·Unity 전체 로그·인증/개인 설정은 Git 대상이 아니다.

## 5d7b18c 교정 전달 동기

- 등록부 Q6–Q11을 기존 생성기로 재생성:176행/dropped0, C7총66/closed39. 실제 브라우저 s19의63셀 전수 일치/표 전체 가시성 확인 후 별도 탭을 닫았다. 덱36장·101263B/자체 오류0/기존 링크 경고1. 문서 게이트514/514 SPEC-PASS, runtimeStatus NOT-MEASURED.
- 원래 cleanup 수치는 JSON의 initialPublicationNote와 위 절로 구분했다. 현재 source/build/test/native 및 중간 epoch는 postPublicationCorrection이 소유한다. 임시 프로덕션 코드/새 producer 없음. 빌드·체크포인트·원시 로그는 stage하지 않는다.
- 기존 vault의 decisions(D-016)/index/log를 직접 파일 fallback으로 갱신했다. Obsidian 실행 성공을 주장하지 않으며 새 raw 수집/유료 의미 추출/외부 발행 없음. 잘못된 PATH의 TeX mex는 실행하지 않는다.
- `graphify update .`: AST2234/2234,39177노드/43404간선/4151community. LLM 불필요 코드 추출만 실행했으며 문서/이미지 의미 추출은 하지 않았다. HTML은5000노드 한도로 생략했다.
- `zg index .` / `zg status`:2075/2075파일,106114entities,191truncated fragments, pending0/failed0. 로컬 potion-code-16m-v2 인덱스이며 무손실 전수 의미 이해를 주장하지 않는다.
- 처방된 `session-start.sh`를 저장소 root와 GAME_OPS_VAULT로 실행했다. `_workspace/current`678 markdown의 frontmatter/supersedes 검사0 findings. cycle-start 미제공으로 age는 미측정이며 이 검사는 memory sync를 검증하지 않는다. mex-agent identity probe unavailable로 명시 skip, Graphify input/output0, zg ready를 보고했다. 전체 G8/mex 성공으로 합치지 않는다.

## 마지막 off-reader 교정

공통 Render에서 Q6 pin 수명을 검사한다. 최신187개 프로젝트 통과와122 PlayMode·올바른 별도 boot1/1, 실제 native112–117/54이벤트·ba14f79e…를 영수증 offReaderLifetimeFollowUp에 연결했다. 첫 boot 인자 누락 skip은 성공으로 세지 않았다. Q6 경계 확장이므로 대장 C7 66/39와 기존 덱63셀은 변하지 않는다. 원래 체크포인트/빌드별 스틸을 보존하고 새 시편의 소유 앱을 종료했다.

최종 동기화: `graphify update .`2238파일/39252노드/43515엣지/4152커뮤니티, LLM토큰0. HTML은5000노드 제한으로 건너뛰었으며 시각화 성공을 주장하지 않는다. `zg index .`와 상태 확인은2083/2083파일·106409엔티티·191잘린조각·pending0/failed0으로 완료했다.

기존 vault의 decisions(D-017)·index·log를 승인된 직접 파일 대체 경로로 동기화했다. session-start의 freshness는680개 Markdown에서0건이며 frontmatter/supersedes 검사만 의미한다. cycle start가 없어 오래됨을 측정하지 않았고 메모리 전체 검증도 아니다. mex-agent는 사용할 수 없어 skip했으며 TeX `mex`를 실행하지 않았다.

7e2c383 전달 시점에는 원시 `.log`53개·로컬 저장 시편·앱 번들을 제외했다. 당시 JSON88283B/SHA-25662fd8ef6…는 이전 버전이다. 현행 JSON의 크기·해시는 위 표이며 변경한 JSON과 이 메타데이터를 함께 새 커밋으로 일반 push한다.

## 최종 소스 검증과 영수증 정합

-7e2c383 생산 소스에서 `editmode-source-final.xml`을 실행해65/65를 확인했다. 외부 예제1개를 제외한 프로젝트64와 기존 최종 PlayMode122/격리 boot1의 고유 합집합은187이다. 이전 실행을 재실행한 것처럼 표시하지 않는다.
-6c88870f epoch 자체에 최종 소스20개의 bytes/SHA와5d7b18c commit을 붙였다. 각 레코드가 그 Git blob과 일치하며 이전35755c5c 소스를 사용하지 않았다.
- 최상위 completedUtc는2026-09-13T17:20:20Z로 마지막 실행17:17:14Z 이후다. sourceBaseCommit은7e2c383이며 런타임 수정·재빌드·사람/생산 gate 변경은 없다. Git 원격 확인은 다음 일반 push의 결과로 기록한다.

후속 동기화는 graphify2238파일/39284노드/43547엣지/4157커뮤니티·LLM토큰0, zg2084/2084파일·106467엔티티·190잘린조각·pending0/failed0으로 완료했다. freshness는680개 Markdown의 frontmatter/supersedes에서0건이다. HTML5000노드 제한·mex-agent 미사용·age 미측정 경계는 동일하다.

JSON95624B/cc64c59e…와 메타데이터 표의 일치, XML51463B/63179940… 및 마지막 실행 이후 완료 시각을 함께 검사했다. 공개 대상은 이 후속 작업의 증거·문서·생성 그래프뿐이며 Git 소유권 점검에서 런타임 변경/다른 작업의 변경은 없었다. 원시 로그53개는 계속 제외한다.
